For windows,

if both    nodemon server.js     or     npx nodemon.js       doesn't work for you.

1. Uninstall nodemon:

     npm uninstall -g nodemon

2. npm install --save-dev nodemon

3. Now,

     npx nodemon server.js
